## Purpose

This module is used to create a linux vm in Azure and connect it to a tailscale network.

## TODOs

- determine best way to store tailscale auth key and translate it into the cloud-init script
